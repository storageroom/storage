[Unit]
Description=Flexget Git Upload
After=network.target

[Service]
User=root
Group=root
WorkingDirectory=/etc/flexget
Type=oneshot
ExecStart=/bin/zsh -c 'git add .;git commit -m "🌈 `date +\"%d/%m\"`";git push origin main'
