[Unit]
Description=Flexget Git Upload
After=network.target

[Timer]
OnCalendar=daily
Persistent=true

[Service]
User=root
Group=root
WorkingDirectory=/etc/flexget
Type=simple
ExecStart=/bin/zsh -c 'git add .;git commit -m "🌈";git push origin main'

[Install]
WantedBy=timers.target